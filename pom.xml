<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.jettra</groupId>
    <artifactId>JettraStoreDriverJava</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
         <jettra.json.version>1.0.0</jettra.json.version>
        <jettra.test.version>1.0.0</jettra.test.version>
        <jettra.jwt.version>1.0.0</jettra.jwt.version>
        <jettra.rules.version>1.0.0</jettra.rules.version>
        <jettra.flux.version>1.0.0-SNAPSHOT</jettra.flux.version>
        <jettra.annotation.version>1.0.0</jettra.annotation.version>
        <jettra.grpc.version>1.0.0</jettra.grpc.version>

    </properties>
    
    <dependencies>
        <!-- Include RestClient, gRPC client dependencies -->
<!--        <dependency>
            <groupId>io.jettra</groupId>
            <artifactId>JettraRest</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>-->
        <dependency>
            <groupId>com.github.jettraframework</groupId>
            <artifactId>JettraGRPC</artifactId>
       <version>${jettra.grpc.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.jettraframework</groupId>
            <artifactId>JettraJSON</artifactId>
            <version>${jettra.json.version}</version>
        </dependency>
         <dependency>
         <groupId>com.github.jettraframework</groupId>
            <artifactId>JettraTest</artifactId>
            <version>${jettra.test.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>25</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>jettra-test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>io.jettra.test.runner.JettraTestRunner</mainClass>
                            <classpathScope>test</classpathScope>
                            <arguments>
                                <argument>${project.build.testOutputDirectory}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
